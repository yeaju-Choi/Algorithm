
numbers = [6,10,9,3]
numbers2 = [100,20,8,3]

lst = []
for i in numbers:
    lst.append(())


# 리스트요소의 첫번째 자리 수를 가지고 정렬해야함 -> 같을 수도?! =
# a = list(map(lambda x:sorted(array[x[0]-1:x[1]])[x[2]-1], ))
# a = sorted(numbers, key=lambda x:x[0])
print(2492//10000)